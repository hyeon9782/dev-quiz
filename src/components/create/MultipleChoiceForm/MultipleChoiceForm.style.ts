import { css } from "@emotion/react";

export const questionInputStyle = css({});

export const optionListStyle = css({
  display: "flex",
  flexDirection: "column",
});

export const optionInputStyle = css({});
