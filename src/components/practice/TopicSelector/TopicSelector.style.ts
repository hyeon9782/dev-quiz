import { css } from "@emotion/react";

export const topicSelectorStyle = css({
  width: "100%",
  display: "flex",
});

export const listStyle = css({
  margin: 0,
  padding: 0,
});

export const itemStyle = css({
  listStyle: "none",
  padding: "10px 20px",
  textAlign: "center",
});
